name: humanize
description: >
  Humanize is a compact toolkit that makes UI strings feel natural. It bridges raw data and human-friendly phrasing with helpers for dates, numbers, lists, and text in English and French.
version: 1.2.1

repository: https://github.com/jeanluc243/humanize
issue_tracker: https://github.com/jeanluc243/humanize/issues

environment:
  sdk: '>=3.4.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0
  intl: ^0.20.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
